{
  "name": "discord-role-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless Discord bot for automated role assignment via interactive dropdowns",
  "scripts": {
    "dev": "npx tsx scripts/dev-server.ts",
    "vercel-dev": "vercel dev",
    "build": "tsc --noEmit",
    "register": "npx tsx scripts/register-commands.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "discord-interactions": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vercel": "^39.0.0"
  }
}
